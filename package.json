{
  "name": "og-game-test-4",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "coverage:check": "vitest run --coverage && node -e \"const cov=require('./coverage/coverage-final.json');const files=Object.keys(cov);let t=0,c=0;files.forEach(f=>{Object.values(cov[f].s).forEach(v=>{t++;if(v>0)c++;})});const pct=(c/t*100).toFixed(2);console.log('Coverage:',pct+'%');if(pct<70){console.error('Coverage below 70% threshold');process.exit(1);}\""
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.9",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
