{
  "name": "og-game-test-4",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "coverage:passing": "vitest run --coverage --exclude 'tests/unit/moon.test.ts' --exclude 'tests/unit/moonBuildings.test.ts' --exclude 'tests/unit/tournament.test.ts'",
    "coverage:ui": "vitest --coverage --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
